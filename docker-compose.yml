version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: carbonbridge-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-carbonuser}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-carbonpass}
      POSTGRES_DB: ${POSTGRES_DB:-carbonbridge}
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-carbonuser} -d ${POSTGRES_DB:-carbonbridge}']
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: carbonbridge-redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: carbonbridge-backend
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      PORT: ${PORT:-4000}
      NODE_ENV: ${NODE_ENV:-production}
      DATABASE_URL: ${DATABASE_URL:-postgresql://${POSTGRES_USER:-carbonuser}:${POSTGRES_PASSWORD:-carbonpass}@postgres:5432/${POSTGRES_DB:-carbonbridge}?schema=public}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379}
      JWT_SECRET: ${JWT_SECRET:-carbonbridge_jwt_production_secret_hackout26_ps8}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET:-carbonbridge_jwt_refresh_production_secret_hackout26_ps8}
      CORS_ORIGIN: ${CORS_ORIGIN:-*}
      UPLOAD_MAX_FILE_SIZE_MB: ${UPLOAD_MAX_FILE_SIZE_MB:-10}
      CRYO_FREIGHT_BASE_RATE_PER_KM: ${CRYO_FREIGHT_BASE_RATE_PER_KM:-1.85}
    ports:
      - '4000:4000'

volumes:
  postgres_data:
  redis_data:
